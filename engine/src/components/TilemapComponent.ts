export default class TilemapComponent {
    constructor(layer?: number, tilesetIndex?: number, tiles?: number[]) {

    }

    layer: number = 0;

    tilesetIndex: number = 0;

    tiles: number[] = [];
}