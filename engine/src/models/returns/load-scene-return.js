var LoadSceneReturn = /** @class */ (function () {
    function LoadSceneReturn(scene, tilesets) {
        this.tilesets = new Array();
        this.scene = scene;
        this.tilesets = tilesets;
    }
    return LoadSceneReturn;
}());
export default LoadSceneReturn;
