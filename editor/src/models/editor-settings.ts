export default class EditorSettings {
    defaultProjectPath: string;
    lastProjectPath: string;
    defaultProjectDirectory: string;
    windowState: string = "maximized";
    windowHeight: number = 1280;
    windowWidth: number = 720;
}